name: bookinfo
resources:
  - namespace:
      name: bookinfo
      labels:
        istio-injection: enabled
      annotations:
        linkerd.io/inject: enabled
  - path: registry/files/bookinfo/appstate.yaml